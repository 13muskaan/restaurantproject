<?php
$id = $_SESSION['member_id'];


$first_name;
$last_name;
$email;
$user_password;

//Use a select statment to select the data in the members table to get the above info
//Something that looks like this: SELECT (first_name, last_name, email, password) FROM member WHERE member_id = $id;

